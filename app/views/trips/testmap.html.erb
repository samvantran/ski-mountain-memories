
<head>


<%= javascript_include_tag 'oms.min' %>
  
  <script>
    

    function initialize() {
      var gm = google.maps;
      var mapCanvas = document.getElementById('map-canvas');
      var lat = 40.704536 //$('#map-canvas').data('lat'); 
      var lng = -74.014711 //$('#map-canvas').data('lng');
      var zoom_level = 8 //$('#map-canvas').data('zoom');
      var visuals = $('#map-canvas').data('visuals');
      
      var mapOptions = {
        center: new google.maps.LatLng(lat, lng),
        zoom: zoom_level,
        mapTypeId: google.maps.MapTypeId.HYBRID
      };

      window.map = new google.maps.Map(mapCanvas, mapOptions); 
    }

function addMarker(visual) {
  var marker = new google.maps.Marker({
    position: new google.maps.LatLng(visual.lat, visual.lng),
    icon: visual.thumbnail_url,
    map: map
  });


}
  google.maps.event.addDomListener(window, 'load', initialize); 

  </script>
</head>
<body>

<div id="map-canvas"></div>
</body>
