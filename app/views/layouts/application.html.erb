<!DOCTYPE html>
<html>

<head>
  <title>SkiMountainMemories</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
  <%= analytics_init if Rails.env.production? %>
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
</head>

<body>

  <div class="container"> 

    <div class="container">
      <div class="main-navbar">
        <button class="navbar-button" type="button" >
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        </a>
      </div>
    </div>

    <ActiveRecord::Relation [#<User id: 1, user_name: "svtizzle", ig_id: 5766394, ig_profile_url: "https://instagramimages-a.akamaihd.net/profiles/pr...", created_at: "2014-12-05 19:25:32", updated_at: "2014-12-05 19:25:32">]>

    <!-- Links on page -->
    <div class="container">
      <div class="navbar-links">
        <ul class="main-links">
        <% if session[:user_id] %>
          <% user = User.find(session[:user_id]) %>
          <li><a href="http://instagram.com/<%= user.user_name %>"><img src="<%= user.ig_profile_url %>"> <%= user.user_name %></a></li>
          <hr>
          <li class="logout-link"><a href="<%= sessions_destroy_path %>">Log Out</a></li>
        <% else %>
          <li><a href="<%= instagram_oauth_connect_path %>">Sign in with Instagram</a></li>
          <hr>
          <li><a href="#">About</a></li>
          <hr>
          <li><a href="#">Team</a></li>
        <% end %>
        </ul>
      </div>
    </div>
    <%= yield %>

    <!-- I think this might work but needs further testing -->
          <% if @links %>
            <% @links.each do |link| %>
            <!-- <li><a href="<%= link.address %>" <%= link.css_class if link.class %>><%= link.phrase %></a></li> -->
            <% end %> 
          <% end %>
  </div>

</body>
</html>

